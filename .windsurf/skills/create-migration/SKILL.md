---
name: create-migration
description: Creates a structured migration file when a change in this repo needs to propagate to another repo. Invoke when a fix or feature conceptually belongs in a dependency rather than here.
---

Read `pipeline/skills/create-migration.md` for the full protocol, then execute it for the change we just discussed.
